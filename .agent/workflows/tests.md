---
description: Ejecución automática de todos los tests del proyecto
---

// turbo
1. Ejecutar los tests con pytest configurando el PYTHONPATH y usando el entorno virtual desde el subdirectorio client:
   `.venv\Scripts\python -m pytest`